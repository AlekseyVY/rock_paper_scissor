import React from 'react';
import { Button, Container } from './rules.style';

function Rules() {
  return (
    <Container>
      <Button>
        rules
      </Button>
    </Container>
  );
}

export default Rules;
